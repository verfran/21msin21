import React from 'react';
import GroupMain from './group/GroupMain';

const FileUpload = (props) => {
    return (
        <div>
            <GroupMain action="FileUpload"/>
        </div>
    );
}

export default FileUpload;